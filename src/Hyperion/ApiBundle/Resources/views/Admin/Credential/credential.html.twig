{% extends 'HyperionApiBundle::hyperion.html.twig' %}
{% import 'HyperionApiBundle:macros:admin.html.twig' as admin %}
{% block body %}
    <div class="container">
        {{ admin.breadcrumb(credential.name  ? : 'New Credential', credential.account) }}
        <h1>{{ credential.name ? : 'New Credential' }}</h1>

        {% if credential.id %}

        {% endif %}

        {{ form(form) }}
    </div>

    {% if credential.id %}
        <div class="container" style="margin-top: 30px">
            <p><a href="{{ path('admin_credential_delete', {'id': credential.id}) }}" class="btn btn-xs btn-danger"
                  onclick="return confirm('Are you sure you want to delete this credential?')">
                    <span class="glyphicon glyphicon-fire"></span> Delete Credential</a></p>
        </div>
    {% endif %}
{% endblock %}