{% extends '::admin.html.twig' %}
{% import '::macros/admin.html.twig' as admin %}
{% block body %}
    <div class="container">
        {{ admin.breadcrumb(project.name, project.account) }}
        <h2>{{ project.name }}</h2>
        <p>{{ admin.bakeStatusLabel(project.bakeStatus) }}</p>

        {{ form(form) }}
    </div>
{% endblock %}